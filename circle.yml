machine:
  pre:
    - bash < <(curl -s -S -L https://raw.githubusercontent.com/moovweb/gvm/master/binscripts/gvm-installer)
    - source $HOME/.gvm/scripts/gvm; gvm install go1.5.1 --binary
  environment:
    IMPORT_PATH: "github.com/influxdb/kapacitor"

dependencies:
  pre:
    # setup ipv6
    - sudo sysctl -w net.ipv6.conf.lo.disable_ipv6=0 net.ipv6.conf.default.disable_ipv6=0 net.ipv6.conf.all.disable_ipv6=0

  override:
    - >
      source $HOME/.gvm/scripts/gvm;
      gvm use "go1.5.1";
      mkdir -p "$GOPATH/src/$IMPORT_PATH"
    - >
      source $HOME/.gvm/scripts/gvm;
      gvm use "go1.5.1";
      rsync -a --delete ./ "$GOPATH/src/$IMPORT_PATH/"

test:
  override:
    - >
      source $HOME/.gvm/scripts/gvm;
      gvm use "go1.5.1";
      make -C "$GOPATH/src/$IMPORT_PATH/" test

deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
      - >
        source $HOME/.gvm/scripts/gvm;
        gvm use "go1.5.1";
        go get github.com/tcnksm/ghr;
        ghr -t $GITHUB_TOKEN -u $CIRCLE_PROJECT_USERNAME -r $CIRCLE_PROJECT_REPONAME `git describe --tags` dist/
